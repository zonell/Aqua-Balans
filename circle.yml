machine:
  environment:
    ANDROID_HOME: /home/Android/Sdk

dependencies:
  cache_directories:
    - ~/.android
    - ~/android
  override:
    - ./environmentSetup.sh

test:
  override:
    - ./gradlew build
